# TASK_TRACKER.md

DevSquad task log. Updated by all agents on assign / start / complete.

## Legend
- [ ] pending
- [~] in progress
- [x] done

## Tasks
- [x] Initialize DevSquad workspace — owner: [PM]
- [x] Inspect state (git, worktree, memory) — owner: [PM]+[BACKEND]
- [x] Apply ProfileCard.tsx vertical redesign + JWT-refresh-on-404 to local main — owner: [FRONTEND]
- [x] Audit LandingScreen.tsx sign-in card vs new vertical style — owner: [FRONTEND]
- [x] Run `npm run lint` + `npm run build` — owner: [QA]
- [x] QA review changes — owner: [QA]
- [x] Commit a — `754a330` feat: vertical compact ProfileCard + matching sign-in card
- [x] Commit b — `47ffb8c` fix: eliminate stale-username races in profile system
- [x] Cleanup worktree `profile-ui-fixes` (auto-pruned, branch already gone)
- [x] Push to origin/main — `3bbd4be..47ffb8c` (4 commits)
- [x] Gitignore TASK_TRACKER.md + IDEA_BACKLOG.md, commit + push — `0d07a12`
- [x] Update memory: mark session_2026-05-15_profile-ui as resolved — owner: [PM]
- [x] Add Notification hook (global ~/.claude/settings.json) — osascript banner + Ping sound on input/approval — owner: [BACKEND]
- [x] Update memory: session log resolved, Bug 3 flipped to fixed, added notification-hook-setup feedback memory, MEMORY.md index updated — owner: [PM]
- [x] Bug 4 — ResultScreen + StreakResultScreen submit session.user.name when signed in — commit `35306d4` — owner: [FRONTEND]
- [x] /api/profile/me endpoint + ProfileCard switch (replaces 404-and-refresh band-aid for Bug 3) — commit `eb1e6f7` — owner: [BACKEND]+[FRONTEND]
- [x] Bug 5 — HistoryTab timeAgo 'just now' for <60s — commit `8e288c2` — owner: [FRONTEND]
- [x] Push round 2 to origin/main — `3738d0c..8e288c2`
- [x] Update memory: Bugs 3/4/5 all marked fixed, MEMORY.md index updated — owner: [PM]
- [x] Browser test localhost — ProfileCard, /api/profile/me 401, result screen pill `Signed in as FAKEit3`, payload `{username:FAKEit3, time_ms:840}`, HistoryTab `just now` — owner: [QA]
- [x] Deploy to production via `vercel --prod` (GitHub auto-deploy hook ไม่ trigger; CLI deploy works) — owner: [BACKEND]
- [x] Prod regression caught: result screen pill showed stale `FAKEit` (session JWT lagging). Root: client uses session.user.name; ProfileCard `update()` is async, race with navigation. — owner: [QA]
- [x] Server-side fix `/api/score` — resolve canonical username from session.user.id, ignore client name when authenticated — commit `2c6c15b` — owner: [BACKEND]
- [x] Country chip on result screens + canonical name display via `/api/profile/me` fetch; `useCountry` now returns `[country, setCountry]` tuple — commit `c4378ed` — owner: [FRONTEND]
- [x] Deploy round 3 — `vercel --prod` ✓
- [x] Verify chip + pill on prod — pill `FAKEit3` (canonical), chip default `🇹🇭`, change→JP→submit body `country:"JP"`, history shows 1312ms JP, server override of `STALE_NAME_X`→`FAKEit3` confirmed — owner: [QA]
- [x] Cleanup prod test pollution via `scripts/cleanup-test-pollution.ts` — ZREM 3 members across `lb:single:{all,week,today}`, ZINCRBY -1 country counters (XX -2, JP -1), DECRBY `lb:stats:plays` -3, DELETE 3 DB rows. Verified history=1 (200ms TH). Commit `5d70032`. — owner: [BACKEND]
- [x] Bug 6 — `/profile/me` server redirect resolves canonical username via session.user.id → DB (was using stale JWT `session.user.name` → 404 after rename) — commit `2acc77c` — owner: [BACKEND]
- [x] JWT throttled DB sync — auth.ts background re-pull username from DB every 5s, cookie rotates on every auth() call; eliminates stale `session.user.name` after rename without manual `update()` — commit `95fa9b8` — owner: [BACKEND]
- [x] Push round 4 — `c4378ed..95fa9b8` (3 commits) + `vercel --prod` deploys
- [x] Prod re-verify after each round — /profile/me redirect, JWT auto-refresh post-reload, cookie persists across multi-nav, post-STALE_MS gap stays canonical, console clean — owner: [QA]
- [x] Memory updates: bugs_auth_profile (Bug 6 fixed), cleanup_script_pattern, jwt_throttled_db_sync; MEMORY.md index — owner: [PM]
- [x] Snapshot DevSquad workspace files — commit `8708967` + push (FF after clearing skip-worktree, re-flagged after) — owner: [PM]
- [x] End-to-end rename test on prod — PATCH FAKEit3→FAKEit4 via /api/profile/settings, session auto-refreshes within 12s of the new auth() call (5s STALE_MS + nav latency), /profile/me + ProfileCard + landing all reflect FAKEit4; rollback FAKEit4→FAKEit3 syncs back equally fast. No client `update()` required. — owner: [QA]
- [x] Settings Change Country — replace 2-char text input with shared `COUNTRIES` dropdown via `src/lib/countries.ts`; CountryChip migrated to same source; Save disabled when unchanged — commit `fb7b5af` — owner: [FRONTEND]
- [x] Verify Change Country on prod — initial XX/Save disabled → select TH → PATCH `{field:"country",value:"TH"}` → DB TH → Save disables on refresh → rollback XX — owner: [QA]
- [x] Username rename rate limit + leaderboard sync — added `users.usernameChangedAt timestamp` column (drizzle-kit push), 24h cooldown returns 429 + nextChangeAt + retryAfterSec, /api/score now writes `userId` in member JSON, `rewriteLeaderboardUsername()` scans `lb:{single,streak}:{all,week,today}` on rename and ZREM old + ZADD new for matching members, scores.username denorm bulk-updated — commit `5c65ed6` — owner: [BACKEND]
- [x] SettingsTab username form — accepts `usernameChangedAt`, shows "Available in Xh Ym" + disables input/Save while cooldown active, 30s tick — commit `5c65ed6` — owner: [FRONTEND]
- [x] Verify rate limit on prod — first rename FAKEit3→FAKEit5 (scanned 28, rewritten 0 — legacy members lack userId, snapshot used older username); immediate retry returns 429 + Retry-After 86382s + nextChangeAt 2026-05-16; UI cooldown "Available in 23h 59m" with disabled input — owner: [QA]
- [x] Auto-save result screens + top-right header — ResultScreen/StreakResultScreen submit on mount (signed-in or cached anon username); Save button removed; anonymous w/o cached name still types + submits via Enter/blur. LandingScreen identity card moved to top-right; "New here? Create a free account" link removed — commit `03c1737` — owner: [FRONTEND]
- [x] Verify auto-save on prod — gameplay → POST `/api/score` fires automatically `{username:"FAKEit5", time_ms:895, country:"XX"}` → 201 with percentile 10 → DOM shows "Score saved to leaderboard ✓", no Save button present — owner: [QA]
- [x] Sitemap + metadataBase for Google Search Console — new `src/app/sitemap.ts` (6 public routes: /, /leaderboard, /login, /signup, /privacy, /terms), `metadataBase: new URL('https://dabpose.fun')` + `alternates.canonical: '/'` in root layout. robots.txt already pointed at /sitemap.xml. Prod sitemap.xml 200, Googlebot fetches all 6 routes 200, no `x-robots-tag: noindex` — commit `5561592` — owner: [BACKEND]
- [x] Total dabs counter `lb:stats:dabs` — /api/score pipeline now INCR `lb:stats:dabs` (+1 single, +count streak); /api/stats MGETs plays + dabs and returns both. `scripts/backfill-total-dabs.ts` seeded `lb:stats:dabs` from ZCARD `lb:single:all` (11) + sum of `lb:streak:all` scores (59) = 70 dabs. Ran backfill before deploy so future INCRs start from 70 — commit `d5a0eae` — owner: [BACKEND]
- [x] UI: "DABS WORLDWIDE" → "PLAYS · DABS" — GlobalCounter (landing centerpiece), LandingScreen inline counter, Leaderboard small-text counter all render `{plays} PLAYS · {dabs} DABS`. First pass used `bucketDabs` vanity rounding (`70+`); second pass dropped the rounding for the real number and uppercased the Leaderboard variant to match landing — commits `d5a0eae`, `d22be4f` — owner: [FRONTEND]
- [x] Verify on prod — /api/stats returns `{totalPlays:13, totalDabs:70}`; landing renders "13 PLAYS · 70 DABS"; leaderboard small text renders "13 PLAYS · 70 DABS" (uppercase, real number). Live counter ticked to 14 plays · 71 dabs after one organic play during testing — owner: [QA]
- [x] `.claude/settings.local.json` updated to `defaultMode: "bypassPermissions"` + broad allow list (Bash/Edit/Write/Read/Glob/Grep/WebFetch/WebSearch wildcards). File is gitignored (per-project, not committed). Destructive-ops confirmation still gated by system-prompt safety rules. — owner: [BACKEND]
- [x] Auth pages microcopy + value props — new `src/components/auth/AuthBrandPanel.tsx` shared between /login + /signup. Renders title + tagline + 3 value-prop rows (Zap/Crown/TrendingUp icons → "Save every score forever", "Climb the global leaderboard", "Track your reaction time") + GlobalCounter. /login header "Sign in" → "Welcome back"; signup link copy "First time? Create an account"; submit button "Let's dab"; forgot-password hint nudges users to Google. /signup header "Create account" → "Join the dab squad"; signin link copy "Got an account?"; username placeholder "Pick a username (3–20 chars)"; submit button "Let's dab"; Terms/Privacy notice below form — commit `c457ffa` — owner: [FRONTEND]
- [x] Verify auth pages on prod — /login renders "Welcome back" + value props + counter "14 PLAYS · 71 DABS"; /signup renders "Join the dab squad" + "By signing up, you agree to our Terms and Privacy"; both pages use shared brand panel correctly — owner: [QA]
- [x] End-to-end prod regression sweep — /, /leaderboard, /login, /signup, /sitemap.xml, /api/stats all green; counter 14/71 consistent across surfaces. Landing counter is client-side-only (not in SSR HTML), so initial DOM scrape misses it — render confirmed via 2.5s wait + innerText query. Expected behaviour, not a defect. — owner: [QA]
- [x] GlobalCounter inline — collapse stacked "14" + "PLAYS · 71 DABS" into one row with `flex items-baseline justify-center gap-2`. Number keeps purple/bold (text-lg), caption stays gray uppercase. Verified visually with getBoundingClientRect (both spans overlap on baseline) — commit `c1d5dcf` — owner: [FRONTEND]
- [x] Full UX/UI audit on prod — desktop + mobile sweeps of /, /leaderboard, /login, /signup, /privacy, /terms, /profile/Seed*. Findings: P0 Privacy/Terms outdated for auth data, P1 profile title generic, P2 leaderboard pollution (countrytest3/testcountry/testcountry2 owned 6/10 slots), P3 mobile auth no value props, P3 forgot-password copy misleads email signups — owner: [QA]
- [x] Privacy/Terms rewrite — disclose Auth.js data (email, bcrypt hash, Google ID, country, session cookie, ephemeral IP rate-limit window); add explicit "What we do NOT collect"; add "Your rights" with 24h rename cadence + email-based deletion; Terms gains "4. Accounts" clause; renumber Disclaimer/Changes — commit `e9e9b4b` — owner: [FRONTEND]
- [x] Profile page metadata — `generateMetadata({ params })` on /profile/[username] yields `@username — Dab Pose` title + description + openGraph.url — commit `e9e9b4b` — owner: [FRONTEND]
- [x] Generalized cleanup script `scripts/cleanup-by-username.ts` — scans every lb sorted set, parses member JSON, ZREM matches, decrements country/play/dab counters by the right amount, then DELETE DB scores + users for the given usernames. Ran for {countrytest3, testcountry, testcountry2}: 6 Redis members removed (3 keys × 6 = 18 ZREMs), 0 DB rows (anonymous submissions), counters plays -6 + dabs -6 + country JP -2 TH -2 XX -2 — commit `e9e9b4b` — owner: [BACKEND]
- [x] Synthetic seed script `scripts/seed-synthetic-players.ts` — generate N players with realistic distributions (time ~ N(600,250) clipped 180–2500, streak counts Poisson-ish 3–25, 30% XX / 70% real country, 1–3 plays per player), batched DB inserts of 500 + Redis pipeline ZADDs + global counter INCRBY. Ran COUNT=10000: 10k users + 20,004 score rows + 51,903 dabs, ~5 min wall time. Final counters totalPlays=20012 totalDabs=51968 on prod — commit `e9e9b4b` — owner: [BACKEND]
- [x] Verify cleanup + seed + new metadata + Privacy/Terms on prod — Privacy h2s ["What we collect","What we do NOT collect","Your rights",...]; Terms has "4. Accounts" + renumbered; /profile/Seed4474_gm9s44 title "@Seed4474_gm9s44 — Dab Pose"; leaderboard counter "20,012 PLAYS · 51,968 DABS"; top 3 = synthetic players at 180ms — owner: [QA]
- [x] Bug: /profile/m3kh tab title said "@m3kh — Dab Pose" but body 404'd. Root: m3kh's leaderboard entries were anonymous submissions (no `userId`), so the username never had a `users` row; `generateMetadata` blindly templated the title from the URL param. Fix: generateMetadata now does the same DB lookup as the page and, when missing, returns "Player not found — Dab Pose" + `robots: { index:false, follow:false }` so search engines don't index orphan profile URLs — commit `b997965` — owner: [BACKEND]
- [x] Full prod wipe — new `scripts/nuke-all.ts` (refuses to run without `CONFIRM=NUKE`) SCANs every `lb:*` key in Redis and DELs in batches, then `DELETE FROM scores` + `DELETE FROM users`. Ran once: 20,009 score rows + 10,001 user rows (10k seed + FAKEit5) + 11 Redis keys cleared. Verified prod /api/stats `{plays:0, dabs:0}` and /api/leaderboard `[]` — commit `3dbcc93` — owner: [BACKEND]
- [x] GSC "อ่าน Sitemap ไม่ได้" — diagnosed: first GSC submission happened before commit `5561592` reached prod, so Google fetched /sitemap.xml while it was still 404 and cached the failure. /sitemap.xml is now HTTP 200 + `application/xml` + 6 URLs. Resolution: user resubmits sitemap (⋮ → Resubmit, or remove + add `sitemap.xml`); Google re-fetches within 24h — owner: [BACKEND]
- [x] /signup form was not sending country in the POST body, so the API defaulted to 'XX' and every Settings tab loaded as "Global". Pulled `useCountry()` into the page and added `country` to the JSON payload — commit `05ec4d6` — owner: [FRONTEND]
- [x] Sign out was only reachable inside the Delete Account confirmation path. Added a standalone "Session" section with a Sign out button above the Danger Zone in SettingsTab — commit `05ec4d6` — owner: [FRONTEND]
- [x] DELETE /api/profile/settings now anonymizes instead of orphaning. Before deleting the `users` row it sets every owned scores.username to "[deleted]" and walks each of the six lb:* sorted sets (single/streak × all/week/today), ZREM + ZADD-ing every member whose userId matches so the leaderboard entry keeps its score / country / id but appears as userId=null, username="[deleted]". Plays/dabs counters are intentionally untouched (user preference: keep score) — commit `05ec4d6` — owner: [BACKEND]
- [x] Google sign-in JWT callback was hardcoding new `users.country = 'XX'`, bypassing the country-detect logic that the credentials path went through. Added `detectCountry()` helper that reads `x-vercel-ip-country` from `next/headers` and uses it when inserting Google-provisioned rows; XX fallback only when the header is missing — commit `9b0a80e` — owner: [BACKEND]
- [x] End-to-end verify on prod (FAKEit Google account, post-fix): signup → /api/profile/me showed `country: "XX"` (created before commit 9b0a80e). After Delete the 6 leaderboard entries flipped to `userId: null` / `username: "[deleted]"`, /profile/FAKEit returned 404. Counter held at 6/6 by design. Fresh Google sign-in after Delete created a new account `a9c3e895-…` with `country: "TH"` ✓ — owner: [QA]
- [x] Reverted the delete-anonymize behavior (commit 05ec4d6 → fcf777d). User wanted deleted accounts to keep their username visible on the leaderboard. DELETE /api/profile/settings is now a plain `DELETE FROM users` — the FK `ON DELETE SET NULL` nulls scores.user_id but the username, country, time/count, and Redis member JSON stay intact. SettingsTab copy updated to match — commit `fcf777d` — owner: [BACKEND]
- [x] /api/score country resolution race fix — useCountry()'s initial state is 'XX' and /api/country/detect resolves asynchronously, so ResultScreen's on-mount auto-submit was firing with body.country='XX'. Server now resolves country as: body.country (if valid + non-XX) → x-vercel-ip-country header → 'XX'. Verified on prod with one play after deploy: new entry recorded country=TH despite the auto-submit race — commit `8b0ee7f` — owner: [BACKEND]
- [x] One-off restore of the 6 anonymized `[deleted]` entries — `scripts/restore-deleted-fakeit.ts` ZREM+ZADDs every `{username:'[deleted]'}` member in single/streak × all/week/today to `{username:'FAKEit', country:'TH'}`, applies country counter delta (-6 XX / +6 TH at all-time + week + today), and UPDATEs the 6 orphan `scores` rows (userId=null, username=`[deleted]`) the same way. 18 Redis rewrites + 6 DB rows. Country dashboard now shows TH=7, XX=1 — commit `71849e1` — owner: [BACKEND]
